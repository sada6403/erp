# Issue Tracker — 20-Issue Bug-Fix Session

Status legend: `PENDING` → not started · `IN PROGRESS` → current turn · `DONE` → fixed & confirmed by client · `BLOCKED` → waiting on client input

| # | Title | Phase | Status | Notes |
|---|-------|-------|--------|-------|
| 1 | Hardcoded/shared PIN `1234` works for every branch | 1 — Auth, Branches, Roles | DONE | Default PIN removed from both seed sites; `auth:pinLogin` now requires `branch_id`; added branch-unique PIN validation + failed-attempt audit log |
| 2 | Login method must differ by role | 1 — Auth, Branches, Roles | DONE | Backend now enforces admin-type roles = email+password only, everyone else = PIN only (mirrors existing `UsersPage.tsx` heuristic) |
| 3 | Remove "Head Office" concept; keep only "Main Branch" | 1 — Auth, Branches, Roles | DONE | Removed all UI labels + seed data; added a guarded migration clearing the old placeholder address on existing installs |
| 4 | A branch is being auto-created; must not be | 1 — Auth, Branches, Roles | DONE | Root cause: post-activation sync pulled the cloud's own Main Branch in as a 2nd row; fixed by re-pointing the local branch id to match on activation |
| 5 | Main Branch admin's email & password must be editable | 1 — Auth, Branches, Roles | DONE | Editing was already unblocked in code; added the two genuinely-missing pieces — current-password confirmation for self-service changes, and email format/uniqueness validation |
| 6 | Super Admin colour selection does not apply on edit | 2 — Theme/Colour | DONE | Confirmed: SuperAdmin portal's Companies → Branding modal showed a stale colour on reopen; fixed by patching row state synchronously on save instead of depending on a background refetch racing the modal close |
| 7 | Product price field: leading `0` does not clear | 3 — Product & Price Entry | DONE | Built a shared `NumberInput` component and rolled it out to all 21 files (~40 fields) that had the `parseFloat(...)\|\|0` collapse-to-zero pattern |
| 8 | Product price cannot be edited after entered once | 3 — Product & Price Entry | DONE | Added a real price-edit control (admin-tier roles only) to the cart, backed by a new `updatePrice` store action and matching server-side authorization in `invoices:create` |
| 9 | Retail ↔ Quotation switch does not work | 3 — Product & Price Entry | DONE | Removed the "clear cart before switching" block; cart now carries over, invoice numbering/prefix updates automatically |
| 10 | Product tab reloads every time you switch tabs | 3 — Product & Price Entry | DONE | Added a Zustand `productsStore` cache (matching existing authStore/cartStore convention); ProductsPage now only refetches on first load or after a real mutation |
| 11 | Remove company-admin 100% max discount | 4 — Discounts | DONE | Product's own configured discount is now the only ceiling, for every role including admin (client-confirmed: role-tier caps removed entirely, not just admin's) |
| 12 | Separate discount area from bill area in UI | 4 — Discounts | DONE | Layout approved then built: Discounts is now its own bordered card, separate from a totals-only Bill Summary card |
| 13 | A held bill cannot be returned/recalled | 5 — Bill Operations | DONE | Found "Hold" never actually saved anything (just cleared the cart); built a real hold/recall mechanism on a new `held_carts` table, separate from invoices |
| 14 | A4 bill print outputs a screenshot, not a real A4 bill | 5 — Bill Operations | BLOCKED | Print mechanism/template/page-options all verified correct in code; symptom is in the native print-preview dialog itself — need a screenshot of it to diagnose further |
| 15 | Stock count unit handling (box vs piece) | 6 — Stock & Suppliers | DONE | Wired existing `product_uom` (base+pack) into Stock Count, Purchase receiving, and POS stock display — box+piece entry converts to base-unit pieces everywhere |
| 16 | Supplier management: cannot type into the field | 6 — Stock & Suppliers | DONE | Built a reusable typeable+creatable combobox (`CreatableSearchSelect`), replaced the PO supplier `<select>`, added server-side duplicate-name check |
| 17 | Merge Agent management into Employee management | 7 — Employee/Agent Management | DONE | Unified into one Employee Management page (Users / Agents / Roles tabs), old `/admin/agents` route removed (redirects); data model unchanged per your confirmed scope |
| 18 | Staff ID should auto-fill the rest of the details | 7 — Employee/Agent Management | DONE | Generalized the existing agent-only lookup into an optional Staff ID autofill available for every role, with a clear not-found state |
| 19 | Position must be a dropdown, new positions creatable | 7 — Employee/Agent Management | DONE | New `positions` table + dropdown (reusing Issue 16's `CreatableSearchSelect`), case-insensitive duplicate prevention, backfilled from existing agent records |
| 20 | Set up Email and SMS | 8 — Notifications | DONE | Added `SMTP_*`/`SMS_*` env-var override layer (documented in `.env.example`) on top of the existing Settings-UI config; wired the confirmed "bill/receipt to customer" trigger — `invoices:create` now fires a non-blocking receipt email (existing `invoiceEmailHtml`/`sendEmail`) to the customer's email on file for completed RETAIL/CREDIT sales, only when Email is enabled; failures are logged, never surfaced to the cashier or allowed to delay the sale |
| 21 | Re-check and correct the invoice/quotation design | 9 — Print, Templates & Notifications (follow-up batch) | DONE | Compared real printed samples (invoice, quotation, blank pre-printed A4 stationery) field-by-field against `buildInvoiceHtml()` — the generated invoice/quotation already matches the code correctly (header, From box, meta box, item columns, Subtotal→Discount→Tax→Total order, payment details, numbering prefixes all confirmed correct); no code defect found. The only real difference is against the physical pre-printed A4 stationery sample (CUSTOMER box, shaded rows, Amount Received/Balance, Prepared By/Received By, Business Reg No placement) — client confirmed that gap is intentionally left for Issue 23's upload-image approach, not a restyle here. No code change made. |
| 22 | Thermal & dot-matrix print fonts unclear/hard to read | 9 — Print, Templates & Notifications (follow-up batch) | DONE | Thermal (58/80mm) compact CSS shrank text to 8-9px in thin Segoe UI — bumped everything to 10-12px, switched thermal specifically to bolder Arial/Helvetica, added font-weight 500 on previously-default-weight rows. Dot-matrix pre-printed mode bumped 9pt→10pt (+semi-bold) and SKU sub-line 8pt→9pt for denser impact-head reproduction. Branded (non-preprinted) dot-matrix and A4/B5 sizes left untouched — already correctly sized. |
| 23 | A4/B5 printing via uploaded image template + field-coordinate mapping | 9 — Print, Templates & Notifications (follow-up batch) | DONE | Extended the existing Advanced Layout Designer instead of building new: added `'b5'` as a 4th design slot end-to-end (own `invoice_b5_custom_layout_json` storage key, own 176×250mm page size, own B5 print page-size — already existed in `printerService.ts`, just unreachable). Fixed the real bug: `resolveInvoiceHtml()` now bakes the uploaded background image into real prints whenever the layout isn't marked "pre-printed" (previously never printed regardless). Generalized the background-upload/pre-printed UI from dot-matrix-only to dot/A4/B5. **Known gap, not in approved scope**: no UI currently lets a cashier/admin actually select `'b5'` as the active print design at print time (POS payment screen's print buttons and the basic Settings page's design picker both still only offer Thermal/Dot/A4) — see notes. |
| 24a | Notification trigger settings (per-event Email/SMS/WhatsApp toggles) | 9 — Print, Templates & Notifications (follow-up batch) | DONE | **Prerequisite bug fixed first**: `emailService.ts`/`smsService.ts`/`whatsappService.ts`/`chitNotifications.ts`/`communications.ts`/`invoices.ts` all read settings via flat `store.get('email_enabled')`-style keys that are never written (Settings UI only ever writes the nested `app_settings` blob) — email/SMS/WhatsApp sending has likely never worked from Settings-configured credentials, only via last session's env-var overrides. Fixed all 6 files to read from `app_settings`. Then built: 5-event × 3-channel (Email/SMS/WhatsApp) toggle grid in Settings → Communications → Notification Triggers, backed by `notificationAllowed()` in `chitNotifications.ts` (requires both the global channel toggle AND the event toggle). Wired into `invoices.ts` (bill + new quotation trigger, including new SMS/WhatsApp bill-receipt sends), `communications.ts`'s scheduler (installment + low-stock), and `chitNotifications.ts`'s `notifyCustomer()` (Smart Buy). Manual "send now" buttons intentionally bypass these toggles. Existing behavior preserved by defaulting already-shipped channels to on; only the brand-new quotation trigger and new bill-SMS/WhatsApp default off. |
| 24b | Per-company SMTP/SMS credentials in SuperAdmin portal | 9 — Print, Templates & Notifications (follow-up batch) | DONE | Stored inside `companies.branding_json` under `smtp` and `sms` sub-objects (no new table). Secrets (SMTP password, SMS API key) encrypted at rest using AES-256-GCM via `backend/lib/secretCrypto.ts` (sharing `BACKUP_ENCRYPTION_KEY`). GET responses mask secrets as `********`; PATCH route preserves existing encrypted secrets when masked input is passed back. Added "SMTP / SMS Credentials" tab to SuperAdmin portal's Edit Company modal (`CompaniesPage.tsx`) with full SMTP and generic HTTP SMS gateway controls, plus instant "Send Test Email" and "Send Test SMS" buttons backed by dedicated backend sub-routes (`/api/superadmin/companies/[id]/email-test` and `sms-test`). Storage + test-send only for now per confirmed scope. |
| 25 | Bills list: View/Print buttons only appear on hover | 10 — Bills UI, Invoice Contrast, Sync & Smart Buy (follow-up batch 3) | DONE | Root cause confined to exactly 2 files (a full-repo search found no other occurrences, narrower than assumed): `BillsPage.tsx` and `TransactionReportPage.tsx` both wrapped their row-action buttons in Tailwind `opacity-0 group-hover:opacity-100 transition-opacity`. Removed the hover-reveal classes from both; buttons are now always visible. |
| 26 | Invoice/receipt text too light — needs solid dark colour | 10 — Bills UI, Invoice Contrast, Sync & Smart Buy (follow-up batch 3) | DONE | In `electron/templates/invoiceTemplates.ts` (shared by real printing and the on-screen preview), 8 secondary-text colour values (`#9ca3af`/`#6b7280`/`#78716c` — SKU line, meta labels, payment/total labels, note text, footer contact) were below normal legibility contrast on white; darkened all to `#4b5563`. Core values (item name, totals) were already solid dark and untouched. Corrected an initial recon guess: the Bills list's separate "View" modal (`InvoiceDetailModal.tsx`) does NOT need changing — it's a dark-theme UI component whose lighter Tailwind classes are the correct contrast choice against a dark background, confirmed via `src/index.css`'s theme-aware colour remapping. |
| 27 | Sync Dashboard: `regions`/`zones` fail with "Unsupported sync table" | 10 — Bills UI, Invoice Contrast, Sync & Smart Buy (follow-up batch 3) | DONE | Both tables' MySQL tenant schema already existed (`backend/lib/auth.ts`'s idempotent tenant migration) — they were simply never added to either sync allowlist (`backend/lib/sync.ts`'s `ALLOWED_TABLES` for push, `electron/services/syncService.ts`'s `globalTables` for pull), unlike structurally identical `positions`/`branches`/`roles`. Confirmed a plain omission; registered both tables the same way. No schema change needed. |
| 28 | Smart Buy: no way to confirm a Bank Transfer payment as received | 10 — Bills UI, Invoice Contrast, Sync & Smart Buy (follow-up batch 3) | DONE | Backend logic already existed and was complete but unused: `chits:contributions:verify`/`pendingTransfers` (`electron/ipc/chits.ts`) already handled balance/credit updates, audit logging, and sync — nothing in the UI called them. Built a new `SmartBuyBankTransfersPage.tsx` (list + Confirm/Reject, following `InstallmentsPage.tsx`'s existing Bank Transfers tab pattern), new route/nav entry gated by the same `RequireSmartBuyAccess` guard every other Smart Buy page uses, and made the Dashboard's "Pending Payments" stat card clickable to it. No new backend code. |
| 29 | Require a password to execute "Clear All Data", set per-company from the Super Admin portal | 11 — Clear-All-Data Password Lock + Multi-Device Forced Wipe (follow-up batch 4) | DONE | Added `companies.clear_data_password_hash`/`clear_data_attempts`/`clear_data_locked_until` (bcrypt, same convention as user login), settable **only** from the Super Admin portal (write-only, confirm-twice UI, mandatory on Create Company; existing companies backfilled with a client-specified default). Verification is server-side only — the hash never reaches any device — via a new device-authenticated endpoint with a 5-attempt/15-minute lockout mirroring the existing login lockout. Danger Zone modal now requires both `DELETE ALL` and the correct password. Extracted the table-delete logic into an exported `wipeLocalTransactionalData()` helper for Issue 30 to reuse. **Found and flagged (not fixed — out of scope): `admin:forceReset` reads `cloud_api_key` without decrypting it, so any auth failure looks identical to a real 401 and may trigger an unintended wipe — needs its own urgent look.** |
| 30 | Broadcast a forced, un-skippable "data cleared — refresh" lock screen to every other device of the same company | 11 — Clear-All-Data Password Lock + Multi-Device Forced Wipe (follow-up batch 4) | DONE | New `data_clear_events` table (tenant MySQL + local SQLite) rides the existing 15-second sync poll (added to `globalTables`/`ALLOWED_TABLES`, same technique as `positions`/`regions`/`zones` — no new real-time channel). Clear All Data now pushes the event directly (bypassing the local `sync_queue`, which is wiped moments later in the same operation) before wiping, and self-acknowledges so the originating device never locks itself. Every other device detects it via two electron-store flags (mirroring the existing `device_activated` pattern), checked at the very start of app boot in `App.tsx` — before the activation gate — rendering a new full-screen `DataClearedLockScreen` (exactly one button) that fully replaces the render tree, so sidebar/routes/keyboard shortcuts structurally cannot exist while locked. Refresh reuses Issue 29's exact wipe routine plus the same "first sync" trigger a newly activated device already uses, and is safely re-runnable from scratch on any failure. |
| 31 | Admin login broken after Clear All Data | 12 — Post-Clear-All-Data Login Fix (follow-up batch 5) | DONE | **31a**: `wipeLocalTransactionalData()` deleted `users`/`branches`/`roles` — structural/config data, not transactional test data. Combined with the wipe also deleting `last_pull_timestamp` (forcing a full cloud re-pull immediately after), this meant the next sync silently replaced local login data with whatever the cloud tenant database happened to have — and the cloud's copy of the real admin's role had *already* drifted to "Cashier" independently of this bug (confirmed via direct DB query, pre-dating this session), which had simply never surfaced before because login always read local data. Fixed by removing `users`/`branches`/`roles` from the wipe entirely — Clear All Data no longer touches who can log in, matching the client's explicit expectation. Also directly repaired the live `natural plantation` company's data (both local SQLite and the cloud tenant DB) — user's role restored to Company Admin, missing "Main Branch" row restored on both sides — with the client's explicit go-ahead before touching production data. Post-clear flow no longer redirects to the Setup Wizard (accounts survive now, so there's nothing to set up) — logs out to the normal login screen instead; Danger Zone UI text updated to match. **31b**: confirmed unrelated to 31a via code tracing (`admin:forceReset` is only ever called from `AppLayout.tsx`'s separate tenant-deletion 401-detection path, and `cloud_api_key`/`cloud_api_url` were never touched by Clear All Data) — but fixed anyway since it's a real, previously-only-flagged bug: `admin:forceReset` was reading `cloud_api_key` without decrypting it (stored encrypted per `settings.ts`'s `SECRET_KEYS`), so every call got a 401 for the wrong reason and the handler treats any 401 as "tenant confirmed deleted," meaning it could wipe local data on a false positive every time it ran. **31c**: confirmed via code tracing (not a live login, no test credentials available) that the SuperAdmin portal's own login (`backend/app/api/auth/superadmin/route.ts`) queries the platform-level `pool` against `pos_erp_saas.superadmins` — architecturally separate from any tenant's isolated MySQL schema or local SQLite; neither Clear All Data nor `forceReset` has any code path that reaches it. |
| 32 | Role-ID drift: local's fixed-constant default role IDs vs cloud's independently-random ones cause a user's role to silently change | 13 — Role-ID Drift Fix + Secure Emergency Support Access (follow-up batch 6) | DONE | Root cause was not the generic pull-side name-matching (already safe) but a **silent guess-fallback** duplicated on both sync directions (pull: `syncService.ts`; push: `backend/lib/sync.ts`'s `resolveRoleId()`) that guessed a replacement role from email/name substrings whenever a `role_id` didn't resolve, defaulting to Cashier — confirmed as the exact mechanism that silently downgraded the real `natural plantation` admin twice (once pre-dating this session, once live during this session's own recon). Fixed at the root: new `electron/services/roleReconcile.ts` (modeled on the existing, proven `branchReconcile.ts`) dynamically discovers every `role_id`-style FK column and re-points local rows from the fixed placeholder to the cloud's real id for the 5 known default roles only (custom roles untouched); wired into both `activation.ts` and `syncService.ts`'s bootstrap-pull path. Both guess-fallbacks hardened to preserve the existing role and `console.warn` instead of guessing. **Data repair**: re-applied the live-recurrence fix to `info@nfplantation.com` — local SQLite `role_id` restored to Company Admin (was corrupted to Cashier again, `updated_at` minutes old); cloud tenant DB checked read-only first and found already correct, so no cloud write was needed. Read-only survey across every tenant schema (`demo`, `natural-plantation-1`) found no other company affected. |
| 33 | Secure, time-limited Super Admin emergency support access (replaces the rejected idea of one shared Super Admin password across every company) | 13 — Role-ID Drift Fix + Secure Emergency Support Access (follow-up batch 6) | DONE | Per-company, single-use, time-boxed support tokens generated from the SuperAdmin portal. Every token: fresh CSPRNG value, sha512-hashed at rest, scoped to exactly one company, shown once, fully audit-logged (generate/redeem/session-end) under the real Super Admin identity — no shared secret exists anywhere in the mechanism. Extended the previously-orphaned `support_sessions` table rather than adding a parallel one. Shipped in v2.6.10, deployed and verified live (backend routes return 401 not 404, portal UI confirmed in the built bundle). |
| 34 | Security incident follow-up: audit whether the 42-hour vulnerable-build window (2026-08-22T15:11Z – 2026-08-24T09:30:52Z, the `admin@pos.local`/`admin123` backdoor + reset-code bug from Issue 32/33's session) resulted in actual exposure | 14 — Security Incident Follow-up (follow-up session 7) | DONE | **34a (read-only audit)**: nginx access logs show 2 external IPs fully downloaded the vulnerable-sized installer (95,378,935 bytes) during the window, plus 4 more partial/range-request fetches — no way to attribute IPs to companies/devices from available server data (no IP column on `pos_devices`, default nginx log format). Searched both tenants' cloud `audit_logs` for the backdoor's distinctive `LOGIN_SUCCESS`/`superadmin_override` signature and for `PASSWORD_RESET_OTP`-family events (the reset-code bug's trail) — zero matches in either tenant, at any time. One alarming-looking signal (the fixed backdoor-account-ID `u9999999-...` showing heavy `LOGIN`/`PRODUCT_PERMANENT_DELETE` activity in both tenants, including within the window) resolved to reason-labeled bulk demo-data cleanup (`"reason": "dummy"` / `"this is demo"`) predating the window in natural-plantation-1's case — read as legitimate admin housekeeping, not exploitation. **Client confirmed** (after reviewing findings): both fixed-ID accounts and both full-download IPs are known/legitimate — no unattributed exposure found. **34b (credential rotation): skipped** per client's explicit instruction, since 34a found no exposure requiring it. Important caveat carried into the record: the actual backdoor login bypass and the reset-code bug are both 100% local to each Electron device (never touch the backend) — any exploitation that happened locally and never synced to the cloud would be invisible to this audit by construction; this limit was stated plainly, not glossed over. |

| 35 | Role-ID drift recurred on natural plantation right after updating to 2.6.11 (`yuvankanthasami4@gmail.com` locked out, "signs in with a PIN" again) | 15 — Post-2.6.11 Role Drift Recurrence (follow-up session 8) | DONE | Read-only check found: cloud had the correct role (Company Admin), local had drifted back to Cashier — confirmed the exact structural gap the client suspected: `reconcileDefaultRolesFromCloud()` (Issue 32a's self-heal) only ran inside `needsBootstrapPull()` (empty local `users` table), so an already-activated device never re-checked after its first-ever bootstrap, only on a truly fresh install/activation. Immediate repair: direct `UPDATE users SET role_id=...` on the live `pos-erp.db` (app confirmed closed first), matched by user id rather than email, restoring Company Admin. **Root-cause nuance**: the existing default-role reconciliation alone would *not* have caught this specific case even running on every startup — local's `role_id` pointed to a role that legitimately exists locally (Cashier's own ID), not a nonexistent placeholder, which is a different failure shape than what 32a's fix targets; the drift's exact trigger moment isn't reconstructable with certainty from available logs (local `audit_logs` doesn't log `role_id` changes, and ordinary incremental pulls don't re-fetch a row whose cloud `updated_at` predates the last pull checkpoint). Fix: `syncService.ts` now runs a full role reconciliation once per app process launch (not gated behind bootstrap) via a new `startupReconcileDone` flag, plus a new `reconcileUserRolesFromCloud()` that does a since-epoch fetch of every cloud user and self-heals any local user whose `role_id` disagrees with cloud's — but only when cloud's value resolves to a role that actually exists locally, and always with a loud `console.warn`, never silently. The "1 pending" sync indicator the client saw had already cleared by the time of the check (all 42 `sync_queue` rows were `status='synced'`, all `audit_logs` inserts — mostly repeated `LOGIN_FAILED` entries from the lockout itself, none a `users`-table push) — noted as unreproducible from current state, with a manual step asking the client to screenshot Sync Monitor if it recurs. Also confirmed from the audit trail: the client successfully used Issue 33's new emergency support-access feature twice as a stopgap while locked out — first real-world validation that it works as intended. Not shipped as an emergency hotfix release — ships in the next normal build. |

| 36 | Phase 1 of device authorization/revocation architecture — a Super Admin deactivating a POS device from the portal had no real effect on an already-activated device (online or offline), only blocked the next activation attempt | 16 — Device Authorization & Remote Revocation (follow-up session 9) | DONE | Started from a 41-section production architecture request; a 3-agent codebase audit found most foundational pieces (schema-per-tenant isolation, cursor-based incremental sync, tombstone deletes, UUID local IDs, correct `contextIsolation`/`nodeIntegration`) already solid, several spec sections (multi-company switching) inapplicable to this one-company-per-device app, and confirmed the one real, HIGH-PRIORITY gap: no per-device authorization enforcement existed anywhere — "deactivate device" only flipped a status column. Scoped this phase to that gap alone; idempotency ledger, version/conflict detection, uninstall data cleanup, and an observability dashboard are explicitly deferred to future sessions. **Decisions confirmed with client**: lightweight per-device identification (new `x-device-id` header alongside the existing shared company API key — noted caveat: not a full per-device security boundary, a leaked company key + guessed device id could still spoof a non-revoked device, but it does close the actual "revoke this one device" gap); 72-hour offline authorization lease; revocation **locks only, never auto-purges local data** (matches Issue 29's established never-destroy-data-without-explicit-confirmation pattern). **Backend**: `pos_devices` gets `authorization_version`/`revoked_at`/`revoked_by`/`revoke_reason` columns; new `resolveDeviceAuthorization()` (`backend/lib/auth.ts`) checks a specific device's status via the new header, failing OPEN (no enforcement) when the header is absent (older client) or the device row can't be found — never fails closed on incomplete data, only on a confirmed 'deactivated' status; enforced on all 4 sync routes (push/changes/deletions/related) and reused the existing 5-minute `/api/brand` license poll as the primary "phone home" detection path (no new endpoint); SuperAdmin's existing deactivate/reactivate/reset device actions now bump `authorization_version` and record who/when/why. **Electron**: `CloudApi` sends `x-device-id` on every request; `licenseService.ts` manages the offline lease and a `device_locked` flag, unlocking automatically on a successful check-in confirming active status; `syncService.ts` skips all sync activity when locked and catches a live `DeviceRevokedError` mid-sync; `activation.ts` resets to a clean authorization state (and forces a fresh bootstrap re-pull) on every activation, never restoring stale prior state — directly satisfies "reactivation must not just restore old data." **UI**: new `DeviceLockedScreen.tsx` reuses `ActivationPage` itself (added an optional `bannerMessage` prop) rather than building a second, simplified reactivation form — no email/password/login option reachable anywhere while locked, matching the "no normal login screen" requirement exactly. Added a defense-in-depth `device_locked` guard on `invoices:create` (the core sale-recording path) as a representative example; broader per-handler hardening is a noted fast-follow, not required for this phase's goal. **Rollout limitation stated explicitly**: a device only becomes subject to this enforcement once it updates to a build containing this fix (has to start sending `x-device-id`) — revoking a device still on an older build behaves as it does today until it updates. Not yet built/deployed as a release — code committed and typechecked only; manual verification (revoke a test device on `demo`, confirm lock within one poll cycle; offline-lease expiry; reactivation clears the lock and forces a fresh sync) still needs to be run before shipping. |

| 37 | Slow product sync (~25-40s per edit to reach branches; ~20-25 min to push a large bulk delete) + product deletes silently never reaching branches at all | 17 — Slow Product Sync + Delete Correctness (follow-up session 10) | IN PROGRESS (36a, 36b done; 36c not started) | Note: the client's brief called this "Issue 36," already taken by the device-authorization work above — logged as 37 instead. **36a (measured, not estimated)**: ran the real 59-table `pullChanges()` loop against the live backend — 7.0s actual network time, 17.7s of mandatory `300ms × 59` throttle sleep, **25.4s total per cycle**, effectively ~30s cadence since the 15s timer skips ticks while a cycle is still running. Root cause: 59 sequential HTTP requests with a fixed per-table delay, run in full every cycle regardless of whether anything changed — not network latency (confirmed fast, ~119ms avg), not payload size. Push-side: `processBatch()`'s `BATCH_SIZE=10` combined with every cycle also paying the full ~25s pull cost meant a 447-item bulk push took ~45 cycles ≈ 20-25+ minutes to fully drain. **36b — client explicitly rejected the initial "just slow" framing and demanded concrete DB verification, which surfaced a genuine, severe, confirmed-live bug**: `natural-plantation-1`'s real 447-product bulk delete (2026-08-27 05:02-05:47) correctly wrote all 447 tombstones to cloud `sync_deletions` and the `/api/sync/deletions` endpoint correctly served them back — the push/cloud side was entirely correct. The bug was client-side: `pullDeletions()` (`syncService.ts`) `break`-ed the entire deletion-apply loop on the first row that failed (this app runs SQLite with `foreign_keys=ON`, so any branch with a local row still referencing a deleted product — a `stocks` row, a `chit_members.redeemed_product_id`, etc. — throws `FOREIGN KEY constraint failed`), which permanently froze `last_deletion_pull_timestamp` — every future cycle re-fetched and re-failed on the exact same row, forever, blocking every OTHER pending deletion too, surviving an app restart (bootstrap-pull reset only clears the separate `last_pull_timestamp`, never this cursor). **Reproduced live** with real production tombstone IDs from the actual incident against a scratch DB copy + the real server: confirmed 0 of 447 applied, cursor stuck. **Fix**: `pullDeletions()` now attempts every entry every cycle (one failure can never again block the rest) and only advances the cursor up to — never past — the first still-failing entry in chronological order, so the blocked one is retried automatically every cycle (self-healing once the local reference clears) while everything else proceeds normally; already-applied entries being harmlessly re-sent on a later cycle is a no-op. **Verified the fix** the same way — re-ran the real reproduction with the corrected algorithm: 446 of 447 applied correctly, only the genuinely-conflicted product remained (correctly retryable), matching the exact expected outcome. **Push throughput fix**: `BATCH_SIZE` raised 10→50 (client explicitly asked this be folded in as the same "admin-side sync is slow" complaint) — cuts a 447-item drain from ~20-25 min to an estimated ~7 min; the per-item `REQUEST_DELAY_MS` throttle was deliberately left untouched (shared-backend rate-limit protection, not what was flagged). **36c (near-instant admin-to-branch propagation) not yet started** — design requires the client's confirmation on a concrete target window before building, per the brief's own gate; not yet built/deployed. |

## Releases

- **v2.6.10** (2026-08-24) — Issues 25–33 batch + backdoor/reset-code removal (commit `f871461`). See `CHANGELOG.md`.
- **v2.6.11** (2026-08-24) — Security response release. No code change; closes out Issue 34's audit (no exposure found, no rotation needed). See `CHANGELOG.md`.
- **v2.6.12** (2026-08-24) — Login reliability fix (Issue 35). Role reconciliation now runs on every app launch, not just first bootstrap; new per-user role consistency self-heal. Verified via a real end-to-end simulation (scratch DB copy + live backend) before release. See `CHANGELOG.md`.
- **v2.6.13** (2026-08-26) — Device authorization & remote revocation, Phase 1 (Issue 36). Ships the device build for the already-deployed backend enforcement. See `CHANGELOG.md`.
- **v2.6.14** (2026-08-27) — Product sync speed + delete correctness (Issue 37): pullDeletions() no longer freezes on one FK conflict, BATCH_SIZE 10→50, new 3s watermark check for near-instant product/stock/category propagation. See `CHANGELOG.md`.
